name: Terraform build
on:
  push:
    branches: [main]
    path:
      - terraform/**
  pull_request:
    branches: [main]
    path:
      - terraform/**

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Initialize terraform
      - run: terraform init
      
