name: Terraform build
on:
  push:
    branches: [main]
    paths: 
      - "terraform/**"
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Initialize terraform
      - run: terraform init
